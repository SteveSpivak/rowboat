# Tasks

- [x] 1.1 Enable issues on `SteveSpivak/rowboat`.
  Done condition: `gh issue list -R SteveSpivak/rowboat` no longer reports that issues are disabled.

- [x] 1.2 Create a dedicated GitHub project for Rowboat.
  Done condition: a Rowboat-specific project exists under `SteveSpivak`.

- [x] 1.3 Create the first truthful workstream issues.
  Done condition: the main completed or in-progress local workstreams are represented by a compact issue set grounded in repo docs.

- [x] 1.4 Record the remote bootstrap locally.
  Done condition: the repo contains a local review/log file stating exactly what GitHub surfaces were created.
