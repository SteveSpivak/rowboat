# Tasks

- [x] 0.1 Write the cockpit agent and automation structure spec set.
  Done condition: the repo has a dedicated spec, plan, and task file for agent/automation ownership.

- [x] 1.1 Write the ownership table for workflow agents, triggers, background agents, schedules, and `n8n-local`.
  Done condition: the repo states the correct owner for each concept without overlap.

- [x] 1.2 Pick the first combined proof flow.
  Done condition: the repo identifies one end-to-end path to implement and verify.

- [ ] 2.1 Implement the minimum proof flow.
  Done condition: one real cross-app automation/tool-routing path works.

- [ ] 2.2 Capture honest runtime proof.
  Done condition: logs, screenshots, and route evidence exist for the chosen flow.
