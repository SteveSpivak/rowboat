# Tasks

- [x] 0.1 Write the local Microsoft app source spec set.
  Done condition: the repo has a clear correction for the local-data requirement.

- [x] 1.1 Collect local filesystem evidence for Outlook, Teams, and OneDrive on this machine.
  Done condition: actual local artifact paths are recorded and classified.

- [x] 1.2 Classify each Microsoft app as usable local source, partial source, or unsupported local source.
  Done condition: the repo has an evidence-based classification instead of an assumption.

- [x] 2.1 Write the UI and taxonomy delta.
  Done condition: the repo states how local Microsoft sources differ from connected accounts.

- [x] 2.2 Pick the first implementation target.
  Done condition: OneDrive folders, Outlook local data, or Teams local data is selected based on evidence.
